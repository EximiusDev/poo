#include "Enemigo.h"

Enemigo::Enemigo() {
	/*
	Sprite CrearSprite::getSprW(int i,bool Flip){if(Flip){return WalkLeft[i].getSpr();}else{ return WalkRight[i].getSpr();}}
	
	JumpLeft[i].createSpr("J"+to_string(i)+".png",-0.5,0.5,xL,300);
	
	JumpLeft[i].setPos(xL,y);
int CrearSprite::getxR(){return xR;}
int CrearSprite::gety(){return y;}
	*/
}

