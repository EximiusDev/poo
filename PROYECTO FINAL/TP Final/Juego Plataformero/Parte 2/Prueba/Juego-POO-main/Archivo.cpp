#include "Archivo.h"
#include <cstring>
#include <fstream>

Archivo::Archivo() {
	/*
	char s[30];
	strcpy(s,"juanito");
	*/
	/*
	fstream Archi("Guardado.dat",ios::binary|ios::in|ios::out|ios::trunc);
	Archi.write((char*)&s,sizeof(s));
	*/
	//f.close();
	
}

